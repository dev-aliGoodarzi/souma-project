export const ReduxErrorHandler = (
  err: any

  // errors type will e here => after i read the document , i will create type of this errors .
) => {
  console.log(err);
  // show error here or toast
};
